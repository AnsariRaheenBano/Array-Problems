#include <iostream>
#include <climits>
#include <cmath>
using namespace std;

int main()
{
    int flag=0;
    int n,i,j;
    cin>>n;
    int a[n];
    for(i=0;i<n;i++)
    {
        cin>>a[i];
    }
    for(i=0;i<n;i++)
    {
        for(j=i + 1;j<n;j++)
        {
            if(a[i]==a[j])
            {
                flag=1;
            }
        }
        
    }
    if(flag==1)
    {
        cout<<"true"<<endl;
    }
    else
    {
           cout<<"false"<<endl;
    }
    return 0;
}
